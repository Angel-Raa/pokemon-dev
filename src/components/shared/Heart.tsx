interface HeartProps {
  isFavorite: boolean;
  onClick: () => void;
}

export const Heart = ({isFavorite, onClick}:HeartProps): React.JSX.Element => {
    return (
        <>
        
        </>
    )
}